# --- LLM provider -----------------------------------------------------------
# Format: "vendor/model-name"  (LiteLLM convention)
LLM_PROVIDER=gemini/gemini-2.0-flash

<<<<<<< HEAD
# --- LLM (required for 'ai' mode) ---
# Use "openai/gpt-4o-mini", "gemini/gemini-2.0-flash", "anthropic/claude-3-5-haiku-20241022"
# "openrouter/google/gemma-3-27b-it:free" hoac "ollama/qwen2.5" cho model local (khong can key).
LLM_PROVIDER=openai/gpt-4o-mini
=======
# --- API keys (set ONLY the one matching your provider) ---------------------
# Each provider reads its own env variable — you don't need to fill them all.

# OpenAI  → https://platform.openai.com/api-keys
>>>>>>> c25e62c736ce29ec617ea0e079b55e84dfb5f09f
OPENAI_API_KEY=sk-...
# Dung khi provider la openrouter/...
OPENROUTER_API_KEY=sk-or-v1-...
# Dung khi provider la gemini/... hoac google/...
GEMINI_API_KEY=
# Dung khi provider la anthropic/...
ANTHROPIC_API_KEY=

# Google Gemini (free tier) → https://aistudio.google.com/apikey
GEMINI_API_KEY=AIzaSy...

# Groq (free tier, very fast) → https://console.groq.com
GROQ_API_KEY=gsk_...

# Cohere → https://dashboard.cohere.com/api-keys
COHERE_API_KEY=...

# Anthropic → https://console.anthropic.com
ANTHROPIC_API_KEY=sk-ant-...

# Ollama (local, no key needed) — just set the provider:
# LLM_PROVIDER=ollama/qwen2.5

# --- Browser ---
# Set to "false" to show the browser window (useful for debugging)
HEADLESS=true
PAGE_TIMEOUT=30000

# --- Output ---
OUTPUT_DIR=output

# --- Chunking (AI mode) ---
# Tokens per chunk sent to the LLM. Smaller = cheaper per call.
CHUNK_TOKEN_THRESHOLD=3000
